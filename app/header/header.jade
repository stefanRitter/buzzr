header.header.ng-hide(ng-controller="appHeaderCtrl" ng-show="isLoggedIn()")
  .header-sidebar-toggle(ng-click="toggleSidebar(); $event.stopPropagation();" ng-class="{'open': open}")
    span
  .header-logo.ng-hide(ng-click="toggleSidebar(); $event.stopPropagation();" ng-show="showLogo()")
    img(src="/img/logo.png")
    strong buzzr
